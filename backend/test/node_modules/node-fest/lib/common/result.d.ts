import { TestCase } from "../types";
export declare class Result {
    markExecuting(tc: TestCase): void;
    pass_count: number;
    fail_count: number;
    total_count: number;
    fail_tests: TestCase[];
    constructor();
    markPass(tc: TestCase): void;
    markFail(tc: TestCase, msg?: string): void;
    printResult(): void;
}
