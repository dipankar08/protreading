import { IObject, TestCase } from '../types';
import { Result } from './result';
export declare abstract class BaseTest {
    context: IObject;
    result: Result;
    cur_tc?: TestCase;
    modifyContextForDebug(): void;
    addMoreOption(p: any): void;
    onBeforeExecuteTest(): Promise<void>;
    onAfterExecuteTest(): Promise<void>;
    parseCommand(): void;
    abstract executeTest(tc: TestCase): Promise<any>;
    buildTestFromFile(filepath: string): TestCase[];
    runAllTestCase(testcase: Array<TestCase>): Promise<void>;
    main(): Promise<void>;
    handleGet(args: string[], ret: Result): Promise<void>;
    handlePOST(args: string[], ret: Result): Promise<void>;
    NetworkCall(method: 'GET' | 'POST', url: string, json: string): string;
}
