export declare enum EBrowserType {
    Chrome = 0,
    Firefox = 1
}
import { WebDriver } from 'selenium-webdriver';
import { IObject } from '../types';
export declare function getWebDriver(context: IObject): Promise<WebDriver>;
