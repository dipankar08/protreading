export declare enum EBrowserType {
    Chrome = 0,
    Firefox = 1
}
import { WebDriver } from 'selenium-webdriver';
import { Context } from './web';
export declare function getWebDriver(context: Context): Promise<WebDriver>;
