export const DRow = ({ children }: any) => {
  return <div style={{ display: "flex", flexDirection: "row" }}>{children}</div>;
};

export const DCol = ({ children }: any) => {
  return <div style={{ display: "flex", flexDirection: "column" }}>{children}</div>;
};
