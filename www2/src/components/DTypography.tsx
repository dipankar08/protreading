import { TProps } from "../libs/types";
export const DTextHeader = ({ onChange, children }: TProps) => {
  return <p style={{ color: "black", fontSize: "30px", fontStyle: "bold" }}>{children}</p>;
};

export const DTextSubHeader = ({ onChange, children }: TProps) => {
  return <p style={{ color: "black", fontSize: "30px", fontStyle: "bold" }}>{children}</p>;
};

export const DTextTitle = ({ onChange, children }: TProps) => {
  return <p style={{ color: "black", fontSize: "30px", fontStyle: "bold" }}>{children}</p>;
};

export const DTextSubTitle = ({ onChange, children }: TProps) => {
  return <p style={{ color: "black", fontSize: "30px", fontStyle: "bold" }}>{children}</p>;
};
